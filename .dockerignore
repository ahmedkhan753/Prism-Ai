# deps / build artifacts (rebuilt inside the image)
node_modules
.next
out
build

# env & secrets — never bake into the image; pass at runtime
.env
.env.local
.env*.local

# vcs / tooling
.git
.gitignore
.github
.vscode
.idea

# docs / misc
README.md
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Dockerfile
.dockerignore
docker-compose.yml
